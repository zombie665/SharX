# In-stack updater: Watchtower uses the stack bridge network only — never network_mode: host.
# Panel → http://watchtower:8080/v1/update. (Host-network panel from install.sh: Watchtower on bridge + 127.0.0.1:8080 published.)
# Set WATCHTOWER_HTTP_API_TOKEN in .env for production (e.g. openssl rand -hex 24).
# Image: beatkind/watchtower — see https://github.com/beatkind/watchtower
services:
  sharx:
    image: registry.konstpic.ru/sharx/sharx:latest
    container_name: sharx_app
    labels:
      com.centurylinklabs.watchtower.enable: "true"
    # hostname: yourhostname
    ports:
      - "2053:2053"   # Web UI
      - "2096:2096"   # Subscriptions
    #   - "443:443"     # Inbound
    volumes:
      - $PWD/cert/:/app/cert/
    environment:
      # Xray settings
      XRAY_VMESS_AEAD_FORCED: "false"
      XUI_ENABLE_FAIL2BAN: "true"
      # XUI_LOG_LEVEL: "debug"
      
      # Web Panel настройки (только через env, не доступны в UI)
      # XUI_WEB_PORT: 2053
      # XUI_WEB_LISTEN: 0.0.0.0
      # XUI_WEB_DOMAIN: panel.example.com
      # XUI_WEB_BASE_PATH: /
      # XUI_WEB_CERT_FILE: /app/cert/fullchain.pem
      # XUI_WEB_KEY_FILE: /app/cert/privkey.pem
      
      # Subscription настройки (только через env, не доступны в UI)
      # XUI_SUB_PORT: 2096
      # XUI_SUB_PATH: /sub/
      # XUI_SUB_DOMAIN: sub.example.com
      # XUI_SUB_CERT_FILE: /app/cert/sub-fullchain.pem
      # XUI_SUB_KEY_FILE: /app/cert/sub-privkey.pem
      
      # PostgreSQL settings
      XUI_DB_HOST: postgres
      XUI_DB_PORT: 5432
      XUI_DB_USER: xui_user
      XUI_DB_PASSWORD: change_this_password
      XUI_DB_NAME: xui_db
      XUI_DB_SSLMODE: disable
      XUI_DOCKER_UPDATER_URL: http://watchtower:8080/v1/update
      XUI_DOCKER_UPDATER_TOKEN: ${WATCHTOWER_HTTP_API_TOKEN:-local-dev-insecure-watchtower-token}
    depends_on:
      postgres:
        condition: service_healthy
      watchtower:
        condition: service_started
    networks:
      - sharx_network
    tty: true
#    network_mode: host
    restart: unless-stopped

  watchtower:
    image: beatkind/watchtower:2.3.2
    container_name: sharx_watchtower
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    command:
      - --http-api-update
    environment:
      WATCHTOWER_HTTP_API_TOKEN: ${WATCHTOWER_HTTP_API_TOKEN:-local-dev-insecure-watchtower-token}
      WATCHTOWER_LABEL_ENABLE: "true"
      WATCHTOWER_CLEANUP: "true"
    labels:
      com.centurylinklabs.watchtower.enable: "false"
    networks:
      - sharx_network

  postgres:
    image: registry.konstpic.ru/sharx/postgres:16-alpine
    container_name: sharx_postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: xui_user
      POSTGRES_PASSWORD: change_this_password
      POSTGRES_DB: xui_db
    volumes:
      - $PWD/postgres_data:/var/lib/postgresql/data
    networks:
      - sharx_network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -h 127.0.0.1 -p 5432 -U xui_user -d xui_db"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

networks:
  sharx_network:
    driver: bridge
