# Node worker (sharxnode)

The standalone **node** service is no longer installed by `install.sh` / `install_ru.sh`. Add and manage remote nodes in the **web panel** (Nodes, Geography, etc.).

To deploy the node worker image yourself (Docker Compose, TLS, API), use the copy in the main codebase:

- [sharx-code/node/README.md](../../sharx-code/node/README.md)
